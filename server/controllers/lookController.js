const { query } = require("../db");
const getAllItems = async (req, res) => {
    
};

module.exports = {
  wardrobesController: {
    createWardrobe,
    getWardrobe,
    updateWardrobeName,
    deleteWardrobe,
  },
};
